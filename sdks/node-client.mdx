# Node Client

Installation and usage:

```bash
npm install @revstack/node
```

```ts
import { Revstack } from "@revstack/node";

const client = new Revstack({ apiKey: process.env.REVSTACK_API_KEY });
const me = await client.users.me();
```

Review error handling and retries in production.
